#!/bin/bash
# DEPRECATED alias. The command is `claude-jam`; this file exists only so that muscle memory and
# installs from before the rename keep working. It adds nothing — same entry, same arguments —
# and it is named in no usage text, no error and no doc except one line in the README.
exec "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/claude-jam" "$@"
